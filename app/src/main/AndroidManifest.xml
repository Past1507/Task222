<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.task222">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Task221Activity"
            android:label="@string/task221_name" />
        <activity
            android:name=".Task213Activity"
            android:label="@string/task213_name" />
        <activity
            android:name=".Task212Activity"
            android:label="@string/task212_name" />
        <activity
            android:name=".Task211Activity"
            android:label="@string/task211_name" />
        <activity
            android:name=".SubsActivity"
            android:label="@string/subs_name" />
        <activity android:name=".PhotoActivity"
            android:label="@string/photo_name"></activity>
    </application>

</manifest>